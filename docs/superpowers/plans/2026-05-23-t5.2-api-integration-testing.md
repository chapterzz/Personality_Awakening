# T5.2 API 集成测试（Golden Path）Implementation Plan

> **审核入口（HTML）**：请用浏览器打开  
> [`2026-05-23-t5.2-api-integration-testing.html`](./2026-05-23-t5.2-api-integration-testing.html)  
> 内含与 Playwright 方案对比、覆盖矩阵、HTTP 旅程、待你选型。

> **For agentic workers:** 用户批准 HTML 计划后，使用 superpowers:executing-plans 或 subagent-driven-development 按 Task 实施。**禁止**在用户批准前修改 `apps/`、`prisma/`、`e2e/` 业务源码。

**Goal:** 在现有 ~58 条 API 集成测基础上，补齐 Golden Path（注册→TestResult→看板、测评→scoring→报告数据、guest vs logged-in），使 `pnpm test:integration` **100% 通过**，满足 T5.2 收口（API 版）。

**Architecture:** 新增 2–3 个 `golden-path-*.e2e-spec.ts` + 1 个 `test/helpers/golden-path-helpers.ts`；模块级 spec 不重复扩写；数据自清理 `e2e-golden-*` 前缀。

**Tech Stack:** Jest、supertest、NestJS TestingModule、Prisma（测试库）、现有 `setup-e2e.ts`。

**Spec:** [2026-05-23-t5.2-api-integration-testing-design.md](../specs/2026-05-23-t5.2-api-integration-testing-design.md)

**T5.1 跳过:** Admin API 403 靠既有 `admin-*.e2e-spec.ts`；不增 Playwright Admin guard。

**门禁:** `pnpm test:gate` = static + unit + integration — **不变**；**不**新增 `test:gate:release`。

---

## 待你选型（详见 HTML 审核页）

| ID       | 名称                                               | 说明                      |
| -------- | -------------------------------------------------- | ------------------------- |
| **G1-A** | 分层 Golden Path（推荐）                           | 2–3 个 spec 文件 + helper |
| **G1-B** | 单文件 mega spec                                   | 不推荐                    |
| **G2-A** | 测评链到 scoring + 可选 finalize→dashboard（推荐） | 第 3 条用例可选           |
| **G2-B** | 每条 assessment 必须到 dashboard                   | 更重、慢                  |
| **G3-A** | sequence 只在 assessment spec 内调用（推荐）       | 少文件                    |
| **G3-B** | 独立 `questionnaire-public.e2e-spec.ts`            | 多 2 条 smoke             |

**本计划按 G1-A + G2-A + G3-A 编写。**

---

## File Map

| 操作   | 路径                                                                                 |
| ------ | ------------------------------------------------------------------------------------ |
| Create | `apps/api/test/helpers/golden-path-helpers.ts`                                       |
| Create | `apps/api/test/golden-path-register-dashboard.e2e-spec.ts`                           |
| Create | `apps/api/test/golden-path-assessment-report.e2e-spec.ts`                            |
| Create | `apps/api/test/golden-path-guest-dashboard.e2e-spec.ts`（P1，可与 Task 3 合并）      |
| Modify | `docs/本地环境调试指南.md`（按需一句：集成测需 Docker Postgres + `pnpm db:migrate`） |

**不修改:** `package.json` 脚本、`e2e/`、Playwright 配置。

---

## Task 1: Golden Path Helpers

**Files:**

- Create: `apps/api/test/helpers/golden-path-helpers.ts`
- Test: `apps/api/test/golden-path-register-dashboard.e2e-spec.ts`（Task 3 引用）

- [ ] **Step 1: 创建 helper 模块**

```typescript
/**
 * API 集成测试辅助：Golden Path 唯一昵称、演示注册链、自适应完成态 seed、清理。
 */
import { INestApplication } from '@nestjs/common';
import { AssessmentMode } from '@prisma/client';
import * as request from 'supertest';
import { randomUUID } from 'crypto';
import { PrismaService } from '../../src/prisma/prisma.service';

export const GOLDEN_PREFIX = 'e2e-golden-';

export function uniqueNickname(suffix: string): string {
  return `${GOLDEN_PREFIX}${suffix}-${randomUUID().slice(0, 8)}`;
}

/** 等价 web `registerWithDemoTestResult`：PUT progress → POST register finalize */
export async function registerWithDemoTestResultViaHttp(
  app: INestApplication,
  opts: { nickname: string; password: string; mbtiType?: string },
): Promise<{ accessToken: string; userId: string }> {
  const sessionId = `e2e-golden-sid-${randomUUID()}`;
  const mbtiType = opts.mbtiType ?? 'INFP';

  await request(app.getHttpServer())
    .put('/progress')
    .query({ mode: 'STANDARD', session_id: sessionId })
    .send({
      progress_data: {
        schema_version: 1,
        mode: 'STANDARD',
        standard: { current_index: 0, answers: { Q1: 'demo' } },
      },
      if_match_revision: 0,
    })
    .expect(200);

  const reg = await request(app.getHttpServer())
    .post('/auth/register')
    .send({
      nickname: opts.nickname,
      password: opts.password,
      guest_session_id: sessionId,
      finalize_submission: true,
      submission: {
        mode: AssessmentMode.STANDARD,
        scores: { demo: true },
        mbti_type: mbtiType,
      },
    })
    .expect(201);

  return {
    accessToken: reg.body.data.access_token as string,
    userId: reg.body.data.user.user_id as string,
  };
}

export async function cleanupGoldenPath(prisma: PrismaService): Promise<void> {
  const users = await prisma.user.findMany({
    where: { nickname: { startsWith: GOLDEN_PREFIX } },
    select: { id: true },
  });
  if (users.length === 0) return;
  const ids = users.map((u) => u.id);
  await prisma.testResult.deleteMany({ where: { userId: { in: ids } } });
  await prisma.temporarySession.deleteMany({
    where: { guestSessionId: { startsWith: 'e2e-golden-sid-' } },
  });
  await prisma.user.deleteMany({ where: { id: { in: ids } } });
}
```

- [ ] **Step 2: 追加 `seedGuestAdaptiveStandardComplete`（supertest 版，移植自 `e2e/helpers/adaptive-standard-seed.ts`）**

在 helper 中增加 `fetchAdaptiveOrderedIds(app, answers?)` 与 `seedGuestAdaptiveStandardComplete(app, sessionId)`，调用 `POST /questionnaire/adaptive-demo-v1/sequence` 与 `PUT /progress`。

- [ ] **Step 3: 运行 TypeScript 编译检查**

Run: `pnpm --filter api exec tsc --noEmit`  
Expected: 无新增错误（helper 被 spec 引用前可能无引用错误）

- [ ] **Step 4: Commit**

```bash
git add apps/api/test/helpers/golden-path-helpers.ts
git commit -m "test(api): add golden path integration helpers"
```

---

## Task 2: Guest 看板 API Smoke

**Files:**

- Create: `apps/api/test/golden-path-guest-dashboard.e2e-spec.ts`

- [ ] **Step 1: 写 spec**

```typescript
/**
 * Golden Path Tier 1：Guest 访问看板公开 API（T5.2 / T3.1）。
 */
import { INestApplication } from '@nestjs/common';
import { Test, TestingModule } from '@nestjs/testing';
import * as request from 'supertest';
import { AppModule } from '../src/app.module';
import { setupOpenApi } from '../src/openapi.setup';

jest.setTimeout(30_000);

describe('Golden Path — Guest Dashboard API (T5.2)', () => {
  let app: INestApplication;

  beforeAll(async () => {
    const moduleFixture: TestingModule = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();
    app = moduleFixture.createNestApplication();
    setupOpenApi(app);
    await app.init();
  });

  afterAll(async () => {
    await app.close();
  });

  it('GET /dashboard/stats：无 JWT 仍 200 且 16 型结构', async () => {
    const res = await request(app.getHttpServer()).get('/dashboard/stats').expect(200);
    expect(res.body.success).toBe(true);
    expect(res.body.data.typeDistribution).toHaveLength(16);
  });

  it('GET /dashboard/my-comparison：无 Authorization → 401', async () => {
    await request(app.getHttpServer()).get('/dashboard/my-comparison').expect(401);
  });
});
```

- [ ] **Step 2: 运行**

Run: `pnpm test:integration -- golden-path-guest-dashboard`  
Expected: **2 passed**

- [ ] **Step 3: Commit**

```bash
git add apps/api/test/golden-path-guest-dashboard.e2e-spec.ts
git commit -m "test(api): golden path guest dashboard API smoke"
```

---

## Task 3: 注册 → TestResult → 看板 Golden Path

**Files:**

- Create: `apps/api/test/golden-path-register-dashboard.e2e-spec.ts`

- [ ] **Step 1: 写 failing spec（3 条用例）**

```typescript
/**
 * Golden Path Tier 2：注册演示 TestResult → 看板 stats / my-comparison（T5.2）。
 */
import { INestApplication } from '@nestjs/common';
import { Test, TestingModule } from '@nestjs/testing';
import * as request from 'supertest';
import { AppModule } from '../src/app.module';
import { setupOpenApi } from '../src/openapi.setup';
import { PrismaService } from '../src/prisma/prisma.service';
import {
  cleanupGoldenPath,
  registerWithDemoTestResultViaHttp,
  uniqueNickname,
} from './helpers/golden-path-helpers';

jest.setTimeout(30_000);

describe('Golden Path — Register to Dashboard (T5.2)', () => {
  let app: INestApplication;
  let prisma: PrismaService;

  beforeAll(async () => {
    const moduleFixture: TestingModule = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();
    app = moduleFixture.createNestApplication();
    setupOpenApi(app);
    prisma = app.get(PrismaService);
    await app.init();
  });

  afterAll(async () => {
    await app.close();
  });

  afterEach(async () => {
    await cleanupGoldenPath(prisma);
  });

  it('registerWithDemoTestResult 链：my-comparison 返回 INFP', async () => {
    const nickname = uniqueNickname('reg');
    const { accessToken } = await registerWithDemoTestResultViaHttp(app, {
      nickname,
      password: 'password-ok-11',
      mbtiType: 'INFP',
    });

    const cmp = await request(app.getHttpServer())
      .get('/dashboard/my-comparison')
      .set('Authorization', `Bearer ${accessToken}`)
      .expect(200);

    expect(cmp.body.data.myType).toBe('INFP');
    expect(cmp.body.data.typeRank).toBeGreaterThanOrEqual(1);
  });

  it('同上链路：stats 类型分布含 INFP', async () => {
    const before = await request(app.getHttpServer()).get('/dashboard/stats').expect(200);
    const infpBefore =
      (before.body.data.typeDistribution as Array<{ type: string; count: number }>).find(
        (t) => t.type === 'INFP',
      )?.count ?? 0;

    const nickname = uniqueNickname('stats');
    await registerWithDemoTestResultViaHttp(app, {
      nickname,
      password: 'password-ok-11',
      mbtiType: 'INFP',
    });

    const after = await request(app.getHttpServer()).get('/dashboard/stats').expect(200);
    const infpAfter = (
      after.body.data.typeDistribution as Array<{ type: string; count: number }>
    ).find((t) => t.type === 'INFP')!.count;
    expect(infpAfter).toBeGreaterThanOrEqual(infpBefore + 1);
  });

  it('纯注册无 TestResult：my-comparison 为 null', async () => {
    const nickname = uniqueNickname('plain');
    const reg = await request(app.getHttpServer())
      .post('/auth/register')
      .send({ nickname, password: 'password-ok-11' })
      .expect(201);

    const cmp = await request(app.getHttpServer())
      .get('/dashboard/my-comparison')
      .set('Authorization', `Bearer ${reg.body.data.access_token}`)
      .expect(200);

    expect(cmp.body.data).toBeNull();
    expect(cmp.body.message).toBe('no_test_result');
  });
});
```

- [ ] **Step 2: 运行**

Run: `pnpm test:integration -- golden-path-register-dashboard`  
Expected: **3 passed**

- [ ] **Step 3: Commit**

```bash
git add apps/api/test/golden-path-register-dashboard.e2e-spec.ts
git commit -m "test(api): golden path register to dashboard"
```

---

## Task 4: 测评 → Scoring → 报告数据 Golden Path

**Files:**

- Modify: `apps/api/test/helpers/golden-path-helpers.ts`（确保 `seedGuestAdaptiveStandardComplete` 已实现）
- Create: `apps/api/test/golden-path-assessment-report.e2e-spec.ts`

- [ ] **Step 1: 写 spec — 标准完成态 → scoring（ENTJ signals，与 e2e report mock 一致）**

```typescript
/**
 * Golden Path Tier 3：测评完成态 progress → POST /scoring/mbti（T5.2 报告 API 数据）。
 */
// ... imports + beforeAll/afterAll 同 Task 3 ...

const ENTJ_SIGNALS = [
  { dimension: 'EI', side: 'E', weight: 3 },
  { dimension: 'SN', side: 'N', weight: 2 },
  { dimension: 'TF', side: 'T', weight: 2 },
  { dimension: 'JP', side: 'J', weight: 2 },
];

it('标准完成态 progress + scoring/mbti 返回 ENTJ 报告字段', async () => {
  const sessionId = `e2e-golden-sid-${randomUUID()}`;
  await seedGuestAdaptiveStandardComplete(app, sessionId);

  const score = await request(app.getHttpServer())
    .post('/scoring/mbti')
    .send({ mode: 'STANDARD', signals: ENTJ_SIGNALS })
    .expect(201);

  expect(score.body.data.mbti_type).toBe('ENTJ');
  expect(score.body.data.scores.EI.winner).toBe('E');
});

it('AVG 收束 progress + scoring/mbti 返回 ISFJ（示例 signals）', async () => {
  // 复用 progress.e2e-spec avgAtClosingBody PUT，再 POST scoring AVG signals
  // 断言 mode === 'AVG' 与 mbti_type === 'ISFJ'
});
```

- [ ] **Step 2: （G2-A）可选第三条 — finalize 后 stats 含类型**

```typescript
it('完成态 + register finalize：stats 聚合含 submission 类型', async () => {
  const sessionId = `e2e-golden-sid-${randomUUID()}`;
  await seedGuestAdaptiveStandardComplete(app, sessionId);
  const nickname = uniqueNickname('fin');
  const reg = await request(app.getHttpServer())
    .post('/auth/register')
    .send({
      nickname,
      password: 'password-ok-11',
      guest_session_id: sessionId,
      finalize_submission: true,
      submission: {
        mode: 'STANDARD',
        scores: { E: 1, I: 0, N: 1, T: 1, J: 1 },
        mbti_type: 'ENTJ',
      },
    })
    .expect(201);

  const cmp = await request(app.getHttpServer())
    .get('/dashboard/my-comparison')
    .set('Authorization', `Bearer ${reg.body.data.access_token}`)
    .expect(200);
  expect(cmp.body.data.myType).toBe('ENTJ');
});
```

- [ ] **Step 3: 运行**

Run: `pnpm test:integration -- golden-path-assessment-report`  
Expected: **2–3 passed**（视 G2 选型）

- [ ] **Step 4: Commit**

```bash
git add apps/api/test/helpers/golden-path-helpers.ts apps/api/test/golden-path-assessment-report.e2e-spec.ts
git commit -m "test(api): golden path assessment to scoring report data"
```

---

## Task 5: 全量集成测 + 文档

**Files:**

- Modify: `docs/本地环境调试指南.md`（按需）

- [ ] **Step 1: 全量 integration**

Run: `pnpm test:integration`  
Expected: **全部 PASS**（既有 ~58 + 新增 7–9）

- [ ] **Step 2: 跑 gate**

Run: `pnpm test:gate`  
Expected: static + unit + integration 全过

- [ ] **Step 3: 本地环境指南（若尚无集成测小节则追加）**

追加一句：`pnpm test:integration` 需 Docker Postgres 已起且已 `pnpm db:migrate`。

- [ ] **Step 4: Commit**

```bash
git add docs/本地环境调试指南.md
git commit -m "docs: note integration test DB prerequisite for T5.2"
```

---

## 验收勾选（阶段 E）

- [ ] `pnpm test:integration` 全部 PASS
- [ ] Golden Path 注册→看板 3 条通过
- [ ] Golden Path 测评→scoring 2+ 条通过
- [ ] Guest 看板 API smoke 2 条通过
- [ ] 既有模块 spec 无回归
- [ ] `test:gate` 未改脚本、无 `test:gate:release`
- [ ] T5.1 缺口已在 design/plan 文档化

---

## 执行选项（批准后）

**Plan complete 后可选：**

1. **Subagent-Driven** — 每 Task 派生子 agent + 任务间 review
2. **Inline Execution** — 本会话用 executing-plans 批量执行

**请用户回复「批准 T5.2 API 计划」或指出 G1/G2/G3 选型变更后再进入阶段 C。**
