# T4.6 题库 CMS Implementation Plan

> **审核入口（HTML + Admin 线框 Demo）**：请用浏览器打开  
> [`2026-05-20-t4.6-questionnaire-cms.html`](./2026-05-20-t4.6-questionnaire-cms.html)  
> 内含验收勾选、管理端线框、跳过 T4.4/T4.5 说明、待你选型。

> **For agentic workers:** 用户批准 HTML 计划后，使用 superpowers:executing-plans 或 subagent-driven-development 按 Task 实施。实施前须 TDD。**禁止**在用户批准前修改 `apps/`、`prisma/`、`e2e/` 业务源码。

**Goal:** 交付标准模式题库 CMS：ADMIN 可 CRUD 问卷/题目/选项及计分元数据，发布/下架后学生端 `GET /questionnaire/:id` 与 T2.7 题序、MBTI 计分一致。

**Architecture:** 内嵌 T4.5-MVP（`RolesGuard` + Seed ADMIN + `/admin/login`）→ `AdminQuestionnaireModule` 写库 API → 共享 `questionnaire-validation` → Next `/admin` 管理 UI；公开 `QuestionnaireModule` 不变。

**Tech Stack:** NestJS 10、Prisma、Next.js 14、Shadcn/UI、Jest、Vitest、supertest、Playwright（可选）。

**Spec:** [2026-05-20-t4.6-questionnaire-cms-design.md](../specs/2026-05-20-t4.6-questionnaire-cms-design.md)

---

## 待你选型（详见 HTML 审核页）

| ID       | 名称                                | 说明                          |
| -------- | ----------------------------------- | ----------------------------- |
| **Q1-A** | 原地编辑 `adaptive-demo-v1`（推荐） | 学生端常量不变；运营改同一 ID |
| **Q1-B** | 多问卷 + 环境变量切换 ID            | 灵活但需改 web 配置与 E2E     |

| ID       | 名称                    | 说明                     |
| -------- | ----------------------- | ------------------------ |
| **D1-A** | 题目硬删（推荐 MVP）    | 实现简单；删前校验无依赖 |
| **D1-B** | 题目 `isEnabled` 软禁用 | 需 Schema 迁移           |

---

## 跳过 T4.4 / T4.5 处理（实施必读）

| 任务 | 本计划动作                                                                                  |
| ---- | ------------------------------------------------------------------------------------------- |
| T4.5 | Task 1–2：`RolesGuard`、`@Roles`、`JwtAuthGuard` 组合；Seed ADMIN；**不** 做通用 CMS 壳审计 |
| T4.4 | 管理 UI 复用全站 Tailwind/Shadcn；表格 `overflow-x-auto`；**不** 开 responsive 专项         |

---

## File Map

| 操作   | 路径                                                                            |
| ------ | ------------------------------------------------------------------------------- |
| Create | `apps/api/src/common/guards/roles.guard.ts`                                     |
| Create | `apps/api/src/common/decorators/roles.decorator.ts`                             |
| Create | `apps/api/src/common/guards/jwt-auth.guard.ts`（若尚无）                        |
| Create | `apps/api/src/questionnaire/questionnaire-validation.ts`                        |
| Create | `apps/api/src/questionnaire/questionnaire-validation.spec.ts`                   |
| Create | `apps/api/src/admin-questionnaire/admin-questionnaire.module.ts`                |
| Create | `apps/api/src/admin-questionnaire/admin-questionnaire.controller.ts`            |
| Create | `apps/api/src/admin-questionnaire/admin-questionnaire.service.ts`               |
| Create | `apps/api/src/admin-questionnaire/dto/*.ts`                                     |
| Create | `apps/api/test/admin-questionnaire.e2e-spec.ts`                                 |
| Modify | `apps/api/src/app.module.ts`                                                    |
| Modify | `prisma/seed.ts` 或 Create `prisma/seed-admin.ts`                               |
| Modify | `package.json`（`db:seed:admin` 可选）                                          |
| Create | `apps/web/src/lib/admin-api.ts`                                                 |
| Create | `apps/web/src/lib/admin-auth.ts`                                                |
| Create | `apps/web/src/app/admin/login/page.tsx`                                         |
| Create | `apps/web/src/app/admin/layout.tsx`                                             |
| Create | `apps/web/src/app/admin/questionnaires/page.tsx`                                |
| Create | `apps/web/src/app/admin/questionnaires/[id]/page.tsx`                           |
| Create | `apps/web/src/components/admin/*.tsx`                                           |
| Create | `apps/web/src/test/questionnaire-validation.spec.ts`（若校验纯函数抽到 shared） |
| Create | `e2e/admin-questionnaire-publish.spec.ts`（P1 可选）                            |

---

## Task 1: 共享校验器（TDD）

**Files:**

- Create: `apps/api/src/questionnaire/questionnaire-validation.ts`
- Create: `apps/api/src/questionnaire/questionnaire-validation.spec.ts`

- [ ] **Step 1: 写失败单测 — 非法 dimension**

```typescript
import { validateOptionScoring, QuestionnaireValidationError } from './questionnaire-validation';

it('rejects invalid side for dimension EI', () => {
  expect(() => validateOptionScoring({ dimension: 'EI', side: 'X', weight: 2 })).toThrow(
    QuestionnaireValidationError,
  );
});
```

- [ ] **Step 2: 运行单测确认 FAIL**

Run: `pnpm --filter api test -- questionnaire-validation.spec`
Expected: FAIL — module not found

- [ ] **Step 3: 实现最小校验**

```typescript
const DIMENSION_SIDES = { EI: ['E', 'I'], SN: ['S', 'N'], TF: ['T', 'F'], JP: ['J', 'P'] } as const;
export class QuestionnaireValidationError extends Error {
  readonly name = 'QuestionnaireValidationError';
}
export function validateOptionScoring(input: {
  dimension: string | null;
  side: string | null;
  weight: number | null;
}): void {
  if (!input.dimension) return;
  const sides = DIMENSION_SIDES[input.dimension as keyof typeof DIMENSION_SIDES];
  if (!sides) throw new QuestionnaireValidationError('invalid_dimension');
  if (!input.side || !sides.includes(input.side))
    throw new QuestionnaireValidationError('invalid_side');
  if (input.weight == null || input.weight < 1 || input.weight > 3)
    throw new QuestionnaireValidationError('invalid_weight');
}
```

- [ ] **Step 4: 单测 PASS**

- [ ] **Step 5: Commit** `test(api): questionnaire validation for CMS scoring fields`

---

## Task 2: T4.5-MVP — RolesGuard + Seed ADMIN

**Files:**

- Create: `apps/api/src/common/decorators/roles.decorator.ts`
- Create: `apps/api/src/common/guards/roles.guard.ts`
- Create: `apps/api/src/common/guards/roles.guard.spec.ts`
- Modify: `prisma/seed.ts` 或 `prisma/seed-admin.ts`

- [ ] **Step 1: 单测 — STUDENT 角色被拒绝**

- [ ] **Step 2: 实现 `@Roles('ADMIN')` + `RolesGuard`（读 JWT payload.role）**

- [ ] **Step 3: Seed 用户 `ppa-admin` / role ADMIN（密码 bcrypt，来自 env）**

Run: `pnpm db:seed`（或 `db:seed:admin`）
Expected: 日志含 admin 用户已 upsert

- [ ] **Step 4: Commit** `feat(api): ADMIN role guard and seed admin user`

---

## Task 3: AdminQuestionnaireModule — 列表与详情

**Files:**

- Create: `apps/api/src/admin-questionnaire/admin-questionnaire.module.ts`
- Create: `apps/api/src/admin-questionnaire/admin-questionnaire.service.ts`
- Create: `apps/api/src/admin-questionnaire/admin-questionnaire.controller.ts`
- Create: `apps/api/test/admin-questionnaire.e2e-spec.ts`

- [ ] **Step 1: 集成测试 — 无 token 401、STUDENT 403、ADMIN 200 列表**

- [ ] **Step 2: `GET /admin/questionnaires` 返回全部（含 `isPublished`）**

- [ ] **Step 3: `GET /admin/questionnaires/:id` 含 questions + options**

- [ ] **Step 4: `pnpm test:integration` PASS**

- [ ] **Step 5: Commit**

---

## Task 4: CRUD 题目/选项 + 发布

- [ ] **Step 1: `POST/PATCH/DELETE` 题目与选项，写库前调用 `validateOptionScoring`**

- [ ] **Step 2: `POST .../publish` 设置 `isPublished=true`, `publishedAt=now()`；发布前校验 screening 题存在**

- [ ] **Step 3: `POST .../unpublish` 设 `isPublished=false`**

- [ ] **Step 4: 集成测试 — 发布后 `GET /questionnaire/:id` 200；下架后 404**

- [ ] **Step 5: Commit**

---

## Task 5: Next.js `/admin` UI

- [ ] **Step 1: `/admin/login` — `POST /auth/login`，非 ADMIN 提示拒绝**

- [ ] **Step 2: `/admin/questionnaires` 列表 + 发布状态徽章**

- [ ] **Step 3: `/admin/questionnaires/[id]` 题目表 + 选项编辑（dimension/side/weight/groupTag）**

- [ ] **Step 4: 发布/下架按钮调用 Admin API**

- [ ] **Step 5: Vitest — `admin-auth` token 存储逻辑（可选）**

- [ ] **Step 6: Commit**

---

## Task 6: 验收与门禁

- [ ] **Step 1: 本地演示 — 修改题干 → 发布 → `/test/standard` 可见新文案**

- [ ] **Step 2: `pnpm test:gate`**

- [ ] **Step 3: 更新 `docs/本地环境调试指南.md`（若有 `SEED_ADMIN_PASSWORD`、admin 入口）**

- [ ] **Step 4: 工作日志 `work-logs/2026-05-20-工作日志.md`**

---

## 验收 Checklist（与 HTML 同步）

- [ ] ADMIN 可登录并 CRUD 题库
- [ ] 非 ADMIN 403
- [ ] 发布/下架与学生端拉取一致
- [ ] T2.7 `groupTag` 字段可维护且 sequence API 正常
- [ ] `pnpm test:gate` 绿
