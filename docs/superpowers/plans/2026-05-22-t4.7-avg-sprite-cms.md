# T4.7 AVG 与精灵文案 CMS Implementation Plan

> **审核入口（HTML + Admin 线框 Demo）**：请用浏览器打开  
> [`2026-05-22-t4.7-avg-sprite-cms.html`](./2026-05-22-t4.7-avg-sprite-cms.html)  
> 内含验收勾选、AVG/精灵管理端线框、T4.6 复用说明、待你选型。

> **For agentic workers:** 用户批准 HTML 计划后，使用 superpowers:executing-plans 或 subagent-driven-development 按 Task 实施。实施前须 TDD。**禁止**在用户批准前修改 `apps/`、`prisma/`、`e2e/` 业务源码。

**Goal:** 交付 AVG 剧情与精灵互动文案 CMS：ADMIN 维护 `demo-avg-v1` 节点 JSON 与全局精灵提示，发布/下架后学生端通过公开 API 拉取，与 `useAvgTest` / `useSpriteInteraction` 格式一致。

**Architecture:** 复用 T4.6 `RolesGuard` 与 `/admin` 壳 → 新增 `SpritePromptConfig` 迁移 → 共享 `avg-script-validation` → 公开 `AvgScriptModule` + `SpritePromptModule` → `AdminAvgCmsModule` → Next 管理 UI → 学生端 API 接入。

**Tech Stack:** NestJS 10、Prisma、Next.js 14、Shadcn/UI、Jest、Vitest、supertest。

**Spec:** [2026-05-22-t4.7-avg-sprite-cms-design.md](../specs/2026-05-22-t4.7-avg-sprite-cms-design.md)

---

## 待你选型（详见 HTML 审核页）

| ID       | 名称                           | 说明                                               |
| -------- | ------------------------------ | -------------------------------------------------- |
| **Q1-A** | 原地编辑 `demo-avg-v1`（推荐） | 与 `DEMO_AVG_SCRIPT_ID`、progress E2E fixture 一致 |
| **Q1-B** | 多脚本 + env 切换              | `NEXT_PUBLIC_AVG_SCRIPT_ID`                        |

| ID       | 名称                               | 说明                            |
| -------- | ---------------------------------- | ------------------------------- |
| **E1-A** | JSON + 校验 + 节点预览（推荐 MVP） | 任务书「JSON 或结构化」最小满足 |
| **E1-B** | 结构化节点表单                     | 工作量大，建议二期              |

| ID       | 名称                                 | 说明                           |
| -------- | ------------------------------------ | ------------------------------ |
| **S1-A** | 独立 `SpritePromptConfig` 表（推荐） | 标准/AVG 共用文案              |
| **S1-B** | 并入 AvgScript JSON                  | 零迁移但不利于标准模式独立更新 |

---

## File Map

| 操作   | 路径                                                          |
| ------ | ------------------------------------------------------------- |
| Create | `prisma/migrations/*_add_sprite_prompt_config/migration.sql`  |
| Modify | `prisma/schema.prisma`                                        |
| Modify | `prisma/seed.ts`                                              |
| Create | `apps/api/src/avg-script/avg-script-validation.ts`            |
| Create | `apps/api/src/avg-script/avg-script-validation.spec.ts`       |
| Create | `apps/api/src/sprite-prompt/sprite-prompt-validation.ts`      |
| Create | `apps/api/src/sprite-prompt/sprite-prompt-validation.spec.ts` |
| Create | `apps/api/src/avg-script/avg-script.module.ts`                |
| Create | `apps/api/src/avg-script/avg-script.controller.ts`            |
| Create | `apps/api/src/avg-script/avg-script.service.ts`               |
| Create | `apps/api/src/sprite-prompt/sprite-prompt.module.ts`          |
| Create | `apps/api/src/sprite-prompt/sprite-prompt.controller.ts`      |
| Create | `apps/api/src/sprite-prompt/sprite-prompt.service.ts`         |
| Create | `apps/api/src/admin-avg-cms/admin-avg-cms.module.ts`          |
| Create | `apps/api/src/admin-avg-cms/admin-avg-cms.controller.ts`      |
| Create | `apps/api/src/admin-avg-cms/admin-avg-cms.service.ts`         |
| Create | `apps/api/src/admin-avg-cms/dto/*.ts`                         |
| Create | `apps/api/test/admin-avg-cms.e2e-spec.ts`                     |
| Modify | `apps/api/src/app.module.ts`                                  |
| Create | `apps/web/src/lib/avg-script-api.ts`                          |
| Create | `apps/web/src/lib/sprite-prompt-api.ts`                       |
| Modify | `apps/web/src/lib/admin-api.ts`（或新建 `admin-avg-api.ts`）  |
| Modify | `apps/web/src/app/admin/layout.tsx`                           |
| Create | `apps/web/src/app/admin/avg-scripts/page.tsx`                 |
| Create | `apps/web/src/app/admin/avg-scripts/[id]/page.tsx`            |
| Create | `apps/web/src/app/admin/sprite-prompts/page.tsx`              |
| Modify | `apps/web/src/app/test/avg/avg-test-client.tsx`               |
| Modify | `apps/web/src/app/test/standard/standard-test-client.tsx`     |

---

## Task 1: Prisma 迁移与 Seed

**Files:**

- Modify: `prisma/schema.prisma`
- Create: migration via `pnpm db:migrate`
- Modify: `prisma/seed.ts`

- [ ] **Step 1: 在 schema 追加 `SpritePromptConfig`**

```prisma
model SpritePromptConfig {
  id              String    @id @default("default")
  hesitationLines Json      @map("hesitation_lines")
  mutexLines      Json      @map("mutex_lines")
  publishedAt     DateTime? @map("published_at")
  isPublished     Boolean   @default(false) @map("is_published")

  @@map("sprite_prompt_configs")
}
```

- [ ] **Step 2: 运行迁移**

Run: `pnpm db:migrate`  
Expected: 新表 `sprite_prompt_configs` 创建成功

- [ ] **Step 3: Seed `demo-avg-v1` 与精灵默认文案**

从 `apps/web/src/data/avg-demo-script.ts` 提取 `nodes`/`backgrounds`/`start_node_id` 写入 `AvgScript`；从 `sprite-lines.ts` 写入 `SpritePromptConfig`；两者 `isPublished: true`。

- [ ] **Step 4: 验证 Seed**

Run: `pnpm db:seed`  
Expected: `avg_scripts` 与 `sprite_prompt_configs` 各有 1 行

- [ ] **Step 5: Commit**

```bash
git add prisma/
git commit -m "feat(t4.7): add SpritePromptConfig and seed AVG demo script"
```

---

## Task 2: 共享校验器（TDD）

**Files:**

- Create: `apps/api/src/avg-script/avg-script-validation.ts`
- Create: `apps/api/src/avg-script/avg-script-validation.spec.ts`
- Create: `apps/api/src/sprite-prompt/sprite-prompt-validation.ts`
- Create: `apps/api/src/sprite-prompt/sprite-prompt-validation.spec.ts`

- [ ] **Step 1: 写失败单测 — 无效 start_node_id**

```typescript
import { validateAvgNodesJson } from './avg-script-validation';

it('rejects when start_node_id missing from nodes', () => {
  expect(() =>
    validateAvgNodesJson({
      start_node_id: 'ghost',
      backgrounds: {},
      nodes: { intro: { kind: 'end', background_key: 'x', lines: [] } },
    }),
  ).toThrow(/start_node_id/);
});
```

- [ ] **Step 2: 运行单测确认 FAIL**

Run: `pnpm --filter api test avg-script-validation.spec.ts`  
Expected: FAIL — module not found

- [ ] **Step 3: 实现 `validateAvgNodesJson`**

校验项：节点 kind、next_id 引用、choice options、background_key 存在、可达 end 节点。

- [ ] **Step 4: 精灵文案失败单测 — 空 hesitation**

```typescript
import { validateSpritePromptPayload } from './sprite-prompt-validation';

it('rejects empty hesitationLines', () => {
  expect(() =>
    validateSpritePromptPayload({ hesitationLines: [], mutexLines: { EI: ['x'] } }),
  ).toThrow(/hesitationLines/);
});
```

- [ ] **Step 5: 实现并跑通全部校验单测**

Run: `pnpm --filter api test avg-script-validation.spec.ts sprite-prompt-validation.spec.ts`  
Expected: PASS

- [ ] **Step 6: Commit**

---

## Task 3: 公开消费 API

**Files:**

- Create: `apps/api/src/avg-script/avg-script.module.ts`
- Create: `apps/api/src/avg-script/avg-script.controller.ts`
- Create: `apps/api/src/avg-script/avg-script.service.ts`
- Create: `apps/api/src/sprite-prompt/sprite-prompt.module.ts`
- Create: `apps/api/src/sprite-prompt/sprite-prompt.controller.ts`
- Create: `apps/api/src/sprite-prompt/sprite-prompt.service.ts`
- Modify: `apps/api/src/app.module.ts`

- [ ] **Step 1: 写 service 单测 — 未发布返回 null**

```typescript
it('getPublishedScript returns null when not published', async () => {
  prisma.avgScript.findFirst.mockResolvedValue(null);
  await expect(service.getPublishedScript('demo-avg-v1')).resolves.toBeNull();
});
```

- [ ] **Step 2: 实现 `AvgScriptService.getPublishedScript`**

组装 `{ script_id: id, ...nodesJson }` 为 `AvgScriptConfig` 形状。

- [ ] **Step 3: 实现 `GET /avg-script/:id`**

未发布 → 404 envelope；已发布 → 200。

- [ ] **Step 4: 实现 `GET /sprite-prompts`**

返回 `{ hesitationLines: string[], mutexLines: Record<string, string[]> }`。

- [ ] **Step 5: 注册 Module 并跑单测**

Run: `pnpm --filter api test avg-script.service.spec.ts sprite-prompt.service.spec.ts`  
Expected: PASS

- [ ] **Step 6: Commit**

---

## Task 4: AdminAvgCmsModule + 集成测

**Files:**

- Create: `apps/api/src/admin-avg-cms/admin-avg-cms.module.ts`
- Create: `apps/api/src/admin-avg-cms/admin-avg-cms.controller.ts`
- Create: `apps/api/src/admin-avg-cms/admin-avg-cms.service.ts`
- Create: `apps/api/src/admin-avg-cms/dto/update-avg-nodes.dto.ts`
- Create: `apps/api/src/admin-avg-cms/dto/update-sprite-prompt.dto.ts`
- Create: `apps/api/test/admin-avg-cms.e2e-spec.ts`

- [ ] **Step 1: 写集成测骨架 — STUDENT 403**

```typescript
it('GET /admin/avg-scripts as STUDENT returns 403', () =>
  request(app.getHttpServer())
    .get('/admin/avg-scripts')
    .set('Authorization', `Bearer ${studentToken}`)
    .expect(403));
```

- [ ] **Step 2: 实现 Admin 列表/详情/PUT nodes/publish/unpublish**

`@UseGuards(JwtAuthGuard, RolesGuard)` + `@Roles(UserRole.ADMIN)`；PUT 前调用 `validateAvgNodesJson`。

- [ ] **Step 3: 实现精灵 PUT + publish**

PUT `/admin/sprite-prompts`；发布前 `validateSpritePromptPayload`。

- [ ] **Step 4: 集成测 — 发布后公开 GET 200**

改 nodes 中某 line text → publish → `GET /avg-script/demo-avg-v1` 含新文案。

- [ ] **Step 5: 跑集成测**

Run: `pnpm test:integration -- admin-avg-cms.e2e-spec.ts`  
Expected: PASS

- [ ] **Step 6: Commit**

---

## Task 5: Next Admin UI

**Files:**

- Modify: `apps/web/src/app/admin/layout.tsx`
- Modify: `apps/web/src/lib/admin-api.ts`
- Create: `apps/web/src/app/admin/avg-scripts/page.tsx`
- Create: `apps/web/src/app/admin/avg-scripts/[id]/page.tsx`
- Create: `apps/web/src/app/admin/sprite-prompts/page.tsx`

- [ ] **Step 1: 扩展 layout 顶栏**

链接：`/admin/questionnaires`、`/admin/avg-scripts`、`/admin/sprite-prompts`；标题改为「内容 CMS」。

- [ ] **Step 2: avg-scripts 列表页**

表格：id、title、isPublished、节点数（从 nodesJson 计算）；操作 → 编辑。

- [ ] **Step 3: avg-scripts/[id] 详情页**

JSON textarea（pretty-print）、「校验并保存」按钮、节点预览表、发布/下架；校验错误展示 API message。

- [ ] **Step 4: sprite-prompts 页**

hesitation 多行输入；EI/SN/TF/JP 四个 textarea；保存 + 发布。

- [ ] **Step 5: 手动冒烟**

Run: `pnpm dev:api` + `pnpm dev:web`  
Expected: ppa-admin 登录后可编辑并发布

- [ ] **Step 6: Commit**

---

## Task 6: 学生端 API 接入

**Files:**

- Create: `apps/web/src/lib/avg-script-api.ts`
- Create: `apps/web/src/lib/sprite-prompt-api.ts`
- Modify: `apps/web/src/app/test/avg/avg-test-client.tsx`
- Modify: `apps/web/src/app/test/standard/standard-test-client.tsx`

- [ ] **Step 1: 实现 `fetchPublishedAvgScript(id)`**

```typescript
export async function fetchPublishedAvgScript(id: string): Promise<AvgScriptConfig> {
  const res = await fetch(`${apiBase()}/avg-script/${encodeURIComponent(id)}`);
  const body = await res.json();
  if (!res.ok || !body.success) throw new Error(body.message ?? 'avg_script_fetch_failed');
  return body.data as AvgScriptConfig;
}
```

- [ ] **Step 2: avg-test-client 改为 mount 时 fetch**

常量 `DEMO_AVG_SCRIPT_ID` 保留为 id；loading 态已有；失败展示重试。

- [ ] **Step 3: 实现 `fetchPublishedSpritePrompts` + 工厂函数**

```typescript
export function createSpriteLineGetters(prompts: SpritePrompts) {
  return {
    getHesitationLine: () => prompts.hesitationLines[0] ?? '',
    getMutexLine: (d: DimensionTag) => prompts.mutexLines[d]?.[0] ?? '',
  };
}
```

- [ ] **Step 4: standard-test-client 与 avg-test-client 注入 API 文案**

保留 `sprite-lines.ts` 作 fetch 失败 fallback（可选）或单测 mock。

- [ ] **Step 5: 跑 web 单测**

Run: `pnpm --filter web test`  
Expected: 现有 `avg-script.spec.ts`、`use-sprite-interaction.spec.tsx` PASS

- [ ] **Step 6: Commit**

---

## Task 7: 门禁与文档

- [ ] **Step 1: 全量门禁**

Run: `pnpm test:gate`  
Expected: PASS

- [ ] **Step 2: 更新工作日志**

写入 `work-logs/2026-05-22-工作日志.md`（若当日已有则追加 T4.7 计划条目）

- [ ] **Step 3: Commit（若有文档变更）**

---

## 自审清单

- [ ] Spec §5 API 路由均有对应 Task
- [ ] `validateAvgNodesJson` 覆盖 dialogue/choice/end 与发布前可达性
- [ ] 精灵四维 mutex 键完整
- [ ] 学生端 `script_mismatch` 行为未破坏
- [ ] 无 TBD / 占位步骤

---

## 执行选项（批准后）

**Plan complete and saved to `docs/superpowers/plans/2026-05-22-t4.7-avg-sprite-cms.md`. Two execution options:**

1. **Subagent-Driven（推荐）** — 每 Task 独立 subagent + 任务间 review
2. **Inline Execution** — 本会话 executing-plans 批量执行 + checkpoint

**请回复「批准 T4.7 计划」并选择执行方式后进入阶段 C。**
