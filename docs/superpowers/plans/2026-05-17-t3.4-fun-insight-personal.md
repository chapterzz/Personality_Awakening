# T3.4 趣味数据卡片与个人对比 Implementation Plan

> **审核入口（HTML + 线框 Demo）**：请用浏览器打开  
> [`2026-05-17-t3.4-fun-insight-personal.html`](./2026-05-17-t3.4-fun-insight-personal.html)  
> 内含验收勾选、趣味卡/个人对比静态线框、维度条配色方案对比。

> **For agentic workers:** 用户批准 HTML 计划后，使用 superpowers:executing-plans 或 subagent-driven-development 按 Task 实施。实施前须 TDD。**禁止**在用户批准前修改 `apps/` 业务源码。

**Goal:** 满足 T3.4 验收——趣味四卡数据准确、维度条比例正确、未登录不展示个人对比；补齐 Vitest 与空数据态。

**Architecture:** 信任 T3.1 API；前端提取纯函数 + 组件单测；UI 对齐 T3.2/T3.3 的 clay 卡片与 chart CSS 变量。

**Tech Stack:** Next.js 14、Vitest、Testing Library、现有 `FunInsights` / `MyComparison` 类型。

**Spec:** [2026-05-17-t3.4-fun-insight-personal-design.md](../specs/2026-05-17-t3.4-fun-insight-personal-design.md)

---

## 待你选型（详见 HTML 审核页）

### 维度条配色（二选一）

| ID      | 名称                | 说明                                             |
| ------- | ------------------- | ------------------------------------------------ |
| **D-A** | 双 chart 色（推荐） | 左 `--chart-1`、右 `--chart-2`，与热力图行色一致 |
| **D-B** | 单色 primary        | 维持现状，仅左侧填充 primary                     |

### 个人对比徽章（二选一）

| ID      | 名称             | 说明                           |
| ------- | ---------------- | ------------------------------ |
| **P-A** | 紫渐变圆（推荐） | 与现稿一致，`chart-3` 描边可选 |
| **P-B** | 方角 chip        | 紧凑，移动端省高               |

---

## File Map

| 操作     | 路径                                                        |
| -------- | ----------------------------------------------------------- |
| Modify   | `apps/web/src/components/dashboard/fun-insight-cards.tsx`   |
| Create   | `apps/web/src/test/fun-insight-cards.spec.tsx`              |
| Modify   | `apps/web/src/components/dashboard/personal-comparison.tsx` |
| Create   | `apps/web/src/test/personal-comparison.spec.tsx`            |
| 预计不改 | `apps/web/src/app/dashboard/page.tsx`（已正确挂载）         |
| 预计不改 | `apps/api/src/dashboard/dashboard.service.ts`               |

---

## Task 1: FunInsightCards 纯函数（RED → GREEN）

**Files:**

- Modify: `apps/web/src/components/dashboard/fun-insight-cards.tsx`
- Create: `apps/web/src/test/fun-insight-cards.spec.tsx`

- [ ] **Step 1: 编写失败单测**

```typescript
// apps/web/src/test/fun-insight-cards.spec.tsx
import { describe, expect, it } from 'vitest';
import {
  computeDimensionBarPercent,
  formatInsightDetail,
} from '@/components/dashboard/fun-insight-cards';

describe('computeDimensionBarPercent', () => {
  it('按左右人数比例返回百分比', () => {
    expect(computeDimensionBarPercent(3, 7)).toBe(30);
    expect(computeDimensionBarPercent(5, 0)).toBe(100);
  });
  it('总数为 0 时返回 50', () => {
    expect(computeDimensionBarPercent(0, 0)).toBe(50);
  });
});

describe('formatInsightDetail', () => {
  it('有数据时含人数与百分比', () => {
    expect(formatInsightDetail(18, 14.1)).toBe('18 人（14.1%）');
  });
  it('无数据时返回固定文案', () => {
    expect(formatInsightDetail(0, 0)).toBe('暂无测评数据');
  });
});
```

- [ ] **Step 2: 运行确认 FAIL**

```bash
pnpm --filter web test src/test/fun-insight-cards.spec.tsx
```

Expected: FAIL（函数未导出）

- [ ] **Step 3: 最小实现并导出**

```typescript
export function computeDimensionBarPercent(left: number, right: number): number {
  const total = left + right;
  if (total <= 0) return 50;
  return Math.round((left / total) * 100);
}

export function formatInsightDetail(count: number, percentage: number): string {
  if (count <= 0) return '暂无测评数据';
  return `${count} 人（${percentage}%）`;
}
```

- [ ] **Step 4: 单测 PASS**

```bash
pnpm --filter web test src/test/fun-insight-cards.spec.tsx
```

---

## Task 2: FunInsightCards 组件渲染与空态

**Files:**

- Modify: `apps/web/src/components/dashboard/fun-insight-cards.tsx`
- Modify: `apps/web/src/test/fun-insight-cards.spec.tsx`

- [ ] **Step 1: 追加失败渲染测试**

```typescript
import { render, screen } from '@testing-library/react';
import { FunInsightCards } from '@/components/dashboard/fun-insight-cards';

const mockInsights = {
  mostCommonType: { type: 'INFP', count: 18, percentage: 14.1 },
  rarestType: { type: 'ESTJ', count: 2, percentage: 1.6 },
  mostPopularSprite: { sprite: '月影探索精灵', count: 72 },
  dimensionBalance: { E: 45, I: 55, S: 40, N: 60, T: 48, F: 52, J: 42, P: 58 },
};

describe('FunInsightCards', () => {
  it('渲染四张卡片标题', () => {
    render(<FunInsightCards insights={mockInsights} />);
    expect(screen.getByText('趣味数据')).toBeInTheDocument();
    expect(screen.getByText('最多人的类型')).toBeInTheDocument();
    expect(screen.getByText('最少人的类型')).toBeInTheDocument();
    expect(screen.getByText('最受欢迎精灵')).toBeInTheDocument();
    expect(screen.getByText('维度平衡')).toBeInTheDocument();
  });

  it('展示 API 下发的类型与精灵名', () => {
    render(<FunInsightCards insights={mockInsights} />);
    expect(screen.getByText('INFP')).toBeInTheDocument();
    expect(screen.getByText(/月影探索精灵/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 组件改用 `formatInsightDetail`；`DimensionBar` 改用 `computeDimensionBarPercent`**

- [ ] **Step 3: 按 HTML 所选 **D-A** 或 **D-B** 调整条形色（D-A 示例：左 `var(--chart-1)`，右区用 `color-mix` 或第二 div `var(--chart-2)`）**

- [ ] **Step 4: `pnpm --filter web test src/test/fun-insight-cards.spec.tsx` → PASS**

---

## Task 3: PersonalComparison（RED → GREEN）

**Files:**

- Modify: `apps/web/src/components/dashboard/personal-comparison.tsx`
- Create: `apps/web/src/test/personal-comparison.spec.tsx`

- [ ] **Step 1: 失败单测**

```typescript
import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
import { PersonalComparison } from '@/components/dashboard/personal-comparison';

describe('PersonalComparison', () => {
  it('comparison 为 null 时不渲染', () => {
    const { container } = render(<PersonalComparison comparison={null} />);
    expect(container.firstChild).toBeNull();
  });

  it('有 comparison 时展示类型、占比与排名', () => {
    render(
      <PersonalComparison
        comparison={{ myType: 'INFP', typePercentage: 14.1, typeRank: 1 }}
      />,
    );
    expect(screen.getByText('你的位置')).toBeInTheDocument();
    expect(screen.getByText(/INFP/)).toBeInTheDocument();
    expect(screen.getByText(/14\.1%/)).toBeInTheDocument();
    expect(screen.getByText(/1/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 运行 FAIL → 实现/调整文案（「全体探索者」）→ PASS**

- [ ] **Step 3: 按 HTML 所选 **P-A** / **P-B** 调整徽章样式**

---

## Task 4: 门禁与人工抽检

- [ ] **Step 1: 单元测试**

```bash
pnpm test:unit
```

Expected: 全部 PASS（含新增 2 个 spec 文件）

- [ ] **Step 2: 本地目视（Seed）**

```bash
pnpm db:seed:dashboard
pnpm dev:web
# 访问 http://localhost:3000/dashboard
```

| 场景          | 预期                           |
| ------------- | ------------------------------ |
| 未登录        | 无「你的位置」区块；趣味卡有数 |
| seed 账号登录 | 有个人对比 + 柱图/热力图高亮   |
| 注册无测评    | 无个人对比；琥珀提示仍在       |

- [ ] **Step 3: 对照 [T3.1 人工验收清单](../../T3.1-人工验收清单.md) C-1、C-3、C-4**

---

## 批准格式示例

```
批准 T3.4，维度条 D-A，徽章 P-A
```

---

## Plan 自检（相对 Spec §7）

| Spec 要求    | Task                 |
| ------------ | -------------------- |
| 四张趣味卡   | Task 2               |
| 维度比例     | Task 1–2             |
| 个人对比文案 | Task 3               |
| 未登录隐藏   | Task 3 单测 + Task 4 |
| Vitest 覆盖  | Task 1–3             |

无 TBD /「类似上文」占位。
