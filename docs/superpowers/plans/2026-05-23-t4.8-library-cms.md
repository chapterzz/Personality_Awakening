# T4.8 图书馆内容 CMS Implementation Plan

> **审核入口（HTML + Admin 线框 Demo）**：请用浏览器打开  
> [`2026-05-23-t4.8-library-cms.html`](./2026-05-23-t4.8-library-cms.html)  
> 内含验收勾选、科普文章管理端线框、T4.6/T4.7 复用说明、待你选型。

> **For agentic workers:** 用户批准 HTML 计划后，使用 superpowers:executing-plans 或 subagent-driven-development 按 Task 实施。实施前须 TDD。**禁止**在用户批准前修改 `apps/`、`prisma/`、`e2e/` 业务源码。

**Goal:** 交付科普图书馆 Admin CMS：ADMIN 在后台 CRUD Markdown 文章、分类/标签与发布/下架，学生端已有 `/library` 通过公开 `GET /library/articles` 自动消费。

**Architecture:** 复用 T4.6/T4.7 `RolesGuard` 与 `/admin` 壳 → 共享 `library-validation` → `AdminLibraryCmsModule` 写 `library_articles` → Next 管理 UI；公开 `LibraryModule` **不变**（文章 MVP 零 Schema 迁移）。

**Tech Stack:** NestJS 10、Prisma、Next.js 14、Shadcn/UI、Jest、Vitest、supertest。

**Spec:** [2026-05-23-t4.8-library-cms-design.md](../specs/2026-05-23-t4.8-library-cms-design.md)

---

## 待你选型（详见 HTML 审核页）

| ID       | 名称               | 说明                                      |
| -------- | ------------------ | ----------------------------------------- |
| **V1-A** | 仅文章 MVP（推荐） | 零迁移；对齐 T4.1 与任务书验收            |
| **V1-B** | 含视频元数据       | +`contentType`/`videoUrl` 迁移与学生端 UI |

| ID       | 名称                         | 说明                  |
| -------- | ---------------------------- | --------------------- |
| **S1-A** | 手填 slug + 唯一校验（推荐） | 新建时从标题建议 slug |
| **S1-B** | 完全自动生成 slug            | 易破坏已发布 URL      |

| ID       | 名称                       | 说明                    |
| -------- | -------------------------- | ----------------------- |
| **D1-A** | 已发布须先下架再删（推荐） | DELETE 409 if published |
| **D1-B** | 软删 `isDeleted`           | 需 Schema 迁移          |

**本计划按 V1-A + S1-A + D1-A 编写。** 若选 V1-B，在 Task 1 前插入 Prisma 迁移 Task 0，并追加学生端 `article-card` 改动。

---

## File Map

| 操作   | 路径                                                             |
| ------ | ---------------------------------------------------------------- |
| Create | `apps/api/src/library/library-validation.ts`                     |
| Create | `apps/api/src/library/library-validation.spec.ts`                |
| Create | `apps/api/src/admin-library-cms/admin-library-cms.module.ts`     |
| Create | `apps/api/src/admin-library-cms/admin-library-cms.controller.ts` |
| Create | `apps/api/src/admin-library-cms/admin-library-cms.service.ts`    |
| Create | `apps/api/src/admin-library-cms/dto/create-article.dto.ts`       |
| Create | `apps/api/src/admin-library-cms/dto/update-article.dto.ts`       |
| Create | `apps/api/test/admin-library-cms.e2e-spec.ts`                    |
| Modify | `apps/api/src/app.module.ts`                                     |
| Create | `apps/web/src/lib/admin-library-api.ts`                          |
| Create | `apps/web/src/lib/library-slug.ts`（标题→建议 slug 纯函数）      |
| Create | `apps/web/src/test/library-slug.spec.ts`                         |
| Modify | `apps/web/src/app/admin/layout.tsx`                              |
| Create | `apps/web/src/app/admin/library/articles/page.tsx`               |
| Create | `apps/web/src/app/admin/library/articles/new/page.tsx`           |
| Create | `apps/web/src/app/admin/library/articles/[id]/page.tsx`          |
| Create | `apps/web/src/components/admin/library-article-form.tsx`         |
| Modify | `e2e/library.spec.ts`（P1：Admin 发布流，可选）                  |

---

## Task 1: 共享校验器（TDD）

**Files:**

- Create: `apps/api/src/library/library-validation.ts`
- Create: `apps/api/src/library/library-validation.spec.ts`

- [ ] **Step 1: 写失败单测 — slug 格式**

```typescript
// library-validation.spec.ts
import { validateSlug, SLUG_REGEX } from './library-validation';

describe('validateSlug', () => {
  it('accepts kebab-case slug', () => {
    expect(validateSlug('mbti-basics')).toEqual({ ok: true });
  });

  it('rejects uppercase', () => {
    expect(validateSlug('MBTI-Basics').ok).toBe(false);
  });

  it('rejects empty', () => {
    expect(validateSlug('').ok).toBe(false);
  });
});
```

- [ ] **Step 2: 运行单测确认失败**

Run: `pnpm --filter api test -- library-validation.spec.ts`  
Expected: FAIL — module not found

- [ ] **Step 3: 实现校验器**

```typescript
// library-validation.ts
import { LIBRARY_CATEGORIES, type LibraryCategory } from './library.types';

export const SLUG_REGEX = /^[a-z0-9]+(?:-[a-z0-9]+)*$/;

export type ValidationResult = { ok: true } | { ok: false; message: string };

export function validateSlug(slug: string): ValidationResult {
  const trimmed = slug.trim();
  if (trimmed.length < 2 || trimmed.length > 80) {
    return { ok: false, message: 'slug_length_invalid' };
  }
  if (!SLUG_REGEX.test(trimmed)) {
    return { ok: false, message: 'slug_format_invalid' };
  }
  return { ok: true };
}

export function normalizeTags(raw: string[]): string[] {
  const set = new Set<string>();
  for (const t of raw) {
    const v = t.trim();
    if (v.length >= 1 && v.length <= 32) set.add(v);
  }
  return [...set].slice(0, 10);
}

export function validateCategory(category: string): category is LibraryCategory {
  return (LIBRARY_CATEGORIES as readonly string[]).includes(category);
}

export type ArticleForPublish = {
  title: string;
  slug: string;
  bodyMd: string;
  category: string;
};

export function validateArticleForPublish(article: ArticleForPublish): ValidationResult {
  if (!article.title.trim() || article.title.trim().length > 120) {
    return { ok: false, message: 'title_invalid' };
  }
  const slugResult = validateSlug(article.slug);
  if (!slugResult.ok) return slugResult;
  if (!validateCategory(article.category)) {
    return { ok: false, message: 'category_invalid' };
  }
  if (article.bodyMd.trim().length < 10) {
    return { ok: false, message: 'body_too_short' };
  }
  return { ok: true };
}
```

- [ ] **Step 4: 运行单测确认通过**

Run: `pnpm --filter api test -- library-validation.spec.ts`  
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/library/library-validation.ts apps/api/src/library/library-validation.spec.ts
git commit -m "feat(t4.8): add library article validation helpers"
```

---

## Task 2: AdminLibraryCmsModule（Service + Controller）

**Files:**

- Create: `apps/api/src/admin-library-cms/admin-library-cms.module.ts`
- Create: `apps/api/src/admin-library-cms/admin-library-cms.service.ts`
- Create: `apps/api/src/admin-library-cms/admin-library-cms.controller.ts`
- Create: `apps/api/src/admin-library-cms/dto/create-article.dto.ts`
- Create: `apps/api/src/admin-library-cms/dto/update-article.dto.ts`
- Modify: `apps/api/src/app.module.ts`

- [ ] **Step 1: 写失败集成测骨架（见 Task 3 Step 1）或先写 service 单测**

Service 核心逻辑要点：

- `listArticles(filter?)` — 含未发布，`orderBy: updatedAt desc`（若无 updatedAt 用 `publishedAt desc, title asc`）
- `createArticle(dto)` — slug 查重 → `ConflictException`；默认 `isPublished: false`
- `updateArticle(id, dto)` — slug 变更时查重
- `deleteArticle(id)` — 若 `isPublished` → `ConflictException('article_still_published')`
- `publishArticle(id)` — `validateArticleForPublish` 失败 → `BadRequestException`
- `unpublishArticle(id)` — `isPublished: false`

Admin 详情响应示例（camelCase）：

```typescript
{
  id: string;
  title: string;
  slug: string;
  excerpt: string | null;
  bodyMd: string;
  category: string;
  tags: string[];
  isPublished: boolean;
  publishedAt: string | null;
}
```

- [ ] **Step 2: Controller 路由（对齐 design spec §5.1）**

```typescript
@ApiTags('admin-library')
@ApiBearerAuth()
@Controller('admin/library')
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles(UserRole.ADMIN)
export class AdminLibraryCmsController {
  // GET articles, POST articles, GET/PATCH/DELETE articles/:id
  // POST articles/:id/publish, POST articles/:id/unpublish
}
```

- [ ] **Step 3: 注册 AppModule**

```typescript
// app.module.ts — imports 追加
AdminLibraryCmsModule,
```

- [ ] **Step 4: 本地 smoke**

Run: `pnpm dev:api`  
Expected: Swagger 出现 `admin-library` 标签

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/admin-library-cms/ apps/api/src/app.module.ts
git commit -m "feat(t4.8): add AdminLibraryCmsModule CRUD and publish"
```

---

## Task 3: 集成测试（TDD 门禁）

**Files:**

- Create: `apps/api/test/admin-library-cms.e2e-spec.ts`

- [ ] **Step 1: 写集成测（对齐 admin-questionnaire 模式）**

必覆盖用例：

1. 无 token → 401
2. STUDENT token → 403 on `GET /admin/library/articles`
3. ADMIN 创建草稿 → 200
4. ADMIN 发布 → `GET /library/articles` 含新 slug
5. ADMIN 下架 → `GET /library/articles/:slug` → 404
6. slug 重复 POST → 409
7. 已发布 DELETE → 409

测试数据 prefix：`e2e-admin-lib-`；afterAll 清理 `libraryArticle` + test users。

- [ ] **Step 2: 运行集成测**

Run: `pnpm test:integration -- admin-library-cms.e2e-spec.ts`  
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add apps/api/test/admin-library-cms.e2e-spec.ts
git commit -m "test(t4.8): admin library CMS integration tests"
```

---

## Task 4: Web Admin API 客户端 + 页面

**Files:**

- Create: `apps/web/src/lib/admin-library-api.ts`
- Create: `apps/web/src/lib/library-slug.ts`
- Create: `apps/web/src/test/library-slug.spec.ts`
- Create: `apps/web/src/components/admin/library-article-form.tsx`
- Create: `apps/web/src/app/admin/library/articles/page.tsx`
- Create: `apps/web/src/app/admin/library/articles/new/page.tsx`
- Create: `apps/web/src/app/admin/library/articles/[id]/page.tsx`

- [ ] **Step 1: slug 建议函数单测（TDD）**

```typescript
// library-slug.ts
/** 从中文/英文标题生成建议 slug（小写 kebab-case） */
export function suggestSlugFromTitle(title: string): string {
  return title
    .trim()
    .toLowerCase()
    .replace(/\s+/g, '-')
    .replace(/[^a-z0-9\u4e00-\u9fff-]/g, '')
    .replace(/-+/g, '-')
    .replace(/^-|-$/g, '')
    .slice(0, 80);
}
```

Run: `pnpm --filter web test -- library-slug.spec.ts`

- [ ] **Step 2: admin-library-api.ts**

函数：`fetchAdminLibraryArticles`、`createAdminLibraryArticle`、`fetchAdminLibraryArticle`、`updateAdminLibraryArticle`、`deleteAdminLibraryArticle`、`publishAdminLibraryArticle`、`unpublishAdminLibraryArticle` — 模式复制 `admin-api.ts`（`authHeaders` + `parseEnvelope`）。

- [ ] **Step 3: library-article-form.tsx**

字段：title、slug（onBlur 可填建议）、category `<select>`（三分类中文标签复用 `library-labels.ts`）、tags 逗号输入、excerpt textarea、bodyMd 大 textarea、可选 Markdown 预览 Tab（复用 `article-body.tsx` 或内联 `react-markdown`）。

- [ ] **Step 4: 列表页**

表格列：标题、slug、分类、标签、状态、操作（编辑/发布/下架/删除）。筛选：category + isPublished。顶部「新建文章」→ `/admin/library/articles/new`。

- [ ] **Step 5: 新建/编辑页**

新建 POST 成功后 router.push 到 `[id]`；编辑 PATCH 保存；发布/下架/删除按钮与 T4.6 问卷详情页交互一致。

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/lib/admin-library-api.ts apps/web/src/lib/library-slug.ts apps/web/src/components/admin/ apps/web/src/app/admin/library/
git commit -m "feat(t4.8): admin library articles UI"
```

---

## Task 5: 扩展 Admin 顶栏导航

**Files:**

- Modify: `apps/web/src/app/admin/layout.tsx`

- [ ] **Step 1: 在 AVG/精灵 Link 后追加**

```tsx
<Link
  className={cn(buttonVariants({ variant: 'outline', size: 'sm' }))}
  href="/admin/library/articles"
>
  科普文章
</Link>
```

- [ ] **Step 2: 手动验证**

Run: `pnpm dev:web` + `pnpm dev:api`  
登录 `ppa-admin` → 顶栏可见「科普文章」→ 列表可加载 Seed 文章

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/app/admin/layout.tsx
git commit -m "feat(t4.8): add library articles link to admin nav"
```

---

## Task 6: 门禁与 E2E（P1）

**Files:**

- Modify: `e2e/library.spec.ts`（可选）

- [ ] **Step 1: 全量门禁**

Run: `pnpm test:gate`  
Expected: PASS（static + unit + integration）

- [ ] **Step 2: E2E 发布流（P1，可选）**

在 `e2e/` 新增或扩展：Admin 登录 → 改文章标题 → 发布 → 学生 `/library` 可见。需 E2E Admin 凭据与 API 可达。

- [ ] **Step 3: 最终 Commit**

```bash
git add e2e/
git commit -m "test(t4.8): library CMS publish e2e flow"
```

---

## Spec 覆盖自检

| Spec 要求          | 对应 Task       |
| ------------------ | --------------- |
| Admin CRUD         | Task 2, 4       |
| 发布/下架          | Task 2, 3       |
| RolesGuard 403     | Task 3          |
| library-validation | Task 1          |
| 公开 API 不变      | Task 3 验证 GET |
| Admin UI 线框      | Task 4, HTML §6 |
| test:gate          | Task 6          |

---

## 执行 handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-23-t4.8-library-cms.md`.**

用户批准 HTML 计划（含 V1/S1/D1 选型）后，协调者进入阶段 C：

1. **Subagent-Driven（推荐）** — 每 Task 派发子 agent + 任务间 review
2. **Inline Execution** — 本会话用 executing-plans 批量执行 + checkpoint

**在用户明确回复「批准计划」前，不得修改 `apps/`、`prisma/`、`e2e/` 业务代码。**
