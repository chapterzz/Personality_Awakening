# API (NestJS)
PORT=3001

# Database (T1.2+)
# 与 `prisma/schema.prisma` 一致；本地开发可复制到仓库根 `.env`，或按需覆盖 `prisma/.env`（仓库内已提供占位 URL 供 `pnpm db:validate`）。
# DATABASE_URL="postgresql://user:password@localhost:5432/ppa?schema=public"
