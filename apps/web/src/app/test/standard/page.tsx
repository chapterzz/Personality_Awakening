/**
 * 标准模式测评路由：T2.1 进度条、题卡、选项与自动保存（布局由根 `layout` 的 SiteShell 提供）。
 */
import { StandardTestClient } from './standard-test-client';

export default function StandardTestPage() {
  return <StandardTestClient />;
}
