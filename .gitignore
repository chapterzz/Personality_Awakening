# Dependencies
node_modules
.pnpm-store

# Build outputs
dist
build
out
.next
.turbo
*.tsbuildinfo

# Env（仅忽略仓库根目录；`prisma/.env` 可为占位 URL 供 CLI validate）
/.env
/.env.local
/.env.*.local

# IDE / OS
.idea
.vscode
.DS_Store
Thumbs.db

# Logs & debug
*.log
npm-debug.log*
pnpm-debug.log*

# 每日工作日志（纳入仓库管理）

# Prisma
prisma/*.db
prisma/*.db-journal

# Test / coverage
coverage
playwright-report
test-results
blob-report

# Deploy / local tooling
.vercel

# OS junk
ehthumbs.db
Desktop.ini
