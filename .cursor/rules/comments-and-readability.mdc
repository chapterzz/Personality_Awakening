---
description: 文件头注释、重要逻辑注释与可读性（对齐任务书 §4.1 / TECH_SPEC §6）
alwaysApply: true
---

# 注释与可读性（PPA）

编写或修改手写源码时遵守以下约定（详见 `PPA_Development_Task.md` §4.1、`PPA_Tech_SPEC.md` §6）。

## 文件头

- 每个手写代码文件（如 `.ts` / `.tsx` / `.css`、`prisma/schema.prisma`、必要脚本）在**文件开头**用简短注释说明**本文件职责**。
- 风格与仓库一致：`/** … */`（TS/TSX）、`/* … */`（CSS）、`//`（Prisma 等）。

## 函数与逻辑

- **重要函数、类/方法、非显而易见的业务分支与算法**补充注释：意图、约定或关键约束。
- **不要**对显而易见的 getter/setter、重复样板句逐行堆砌注释。
- **关键算法**（如 MBTI 计分、维度映射）在实现处说明依据（含心理学/PRD 要点）。

## 与「少写废话」的平衡

优先让**命名与结构**自解释；注释用于「读代码仍易误解」处。新增注释使用项目主语言（中文）或与文件现有注释语言一致。
